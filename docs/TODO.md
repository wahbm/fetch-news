# 待办与边界（供下一位 AI）

## P0：生产业务启用前

- 功能提交 `3ac8890` 已完成 CI、迁移和 ECS 发布；上线后的业务端到端验收仍待用受控调用方 key 创建热点、提交批量信息、后台检索并确认最多 3 条通知任务。
- 当前生产未录入测试数据，也未配置真实企业微信群机器人；启用前应先由用户提供测试范围并使用专用测试群，避免未经授权向真实群发送消息。

## P1：产品接入

- 由管理员安全交付真实调用方 key，并配置实际采集任务；调度、来源抓取和 AI 总结仍由调用方系统负责。
- 如需生产通知，创建专用企业微信群机器人并由用户授权测试；验证 key 脱敏、评分前三、限速、失败重试和人工重试。当前生产没有真实订阅方，也没有向真实群发送过消息。
- 可补充浏览器层面的批量评分/前三通知流程；MariaDB 集成测试已覆盖主要并发、事务和 worker 场景。

## P2：运营与演进

- 观察文章、通知和索引增长，以及 Node RSS、swap、磁盘和队列积压；规模上升后评估全文索引或归档策略。
- 在有明确窗口时演练一次上一版本回滚，确认迁移向后兼容；不要用生产库做自动化测试。
- 评估前端首包（当前约 1.18 MB，gzip 约 371 KB）是否需要按页面拆分。

## 明确的非目标

- 后台不实现外部新闻抓取、定时采集、AI 总结、公开注册或调用方创建热点。
- 不引入 Docker、Redis 或第二个队列服务；当前设计依赖 MariaDB 持久化队列和 advisory lock。
- 不承诺企业微信端到端恰好一次；超时重试可能重复送达，尝试记录用于追踪。
- 用户已选择不配置数据库备份；除非用户重新决定，不创建备份任务或修改服务器备份策略。
