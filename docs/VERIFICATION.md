# 本地验证记录

日期：2026-09-13。环境：Node.js 22.21.0、macOS、隔离的本机 MariaDB 12.3.3、Chrome。本节记录本地阶段；远端 CI 和上线验证见下文。

| 验证 | 结果 |
|---|---|
| TypeScript 类型检查、Vite 前端与 tsup 服务端构建 | 通过 |
| 5 项单元测试 | 通过：URL、日期/输入、加密、消息字节限制、WeCom 响应分类 |
| 14 项 MariaDB 集成测试 | 通过：会话/权限、最后管理员、重置 key、停用、搜索、并发去重、事务回滚、订阅匹配、限速、重试、租约恢复、子路径和登录限流 |
| 桌面浏览器完整流程 | 通过：登录、创建热点/调用方/订阅、API 提交与去重、搜索与详情、模拟通知、投递记录、退出 |
| 子路径 `/qa/fetch-news/` | 构建与相同浏览器流程通过，刷新深链接正常 |
| 390px 移动端 | 通过，侧栏折叠、页面无横向溢出，表格内部可横向滚动 |
| 编译产物 smoke test | CLI 迁移、文件传入初始密码、生产服务启动、数据库健康和 SPA 深链接通过 |
| npm audit | 0 已知漏洞（包含开发依赖）；已锁定修复版本 |
| CI/部署模板 | YAML 解析与发布脚本 bash 语法检查通过；未执行 ECS 发布 |
| 企业微信官方说明 | 直接获取官方页面，确认 4096 UTF-8 字节、20 条/分钟及全局错误码 |

所有自动通知测试注入模拟发送器，未向真实企业微信群发消息。截图使用验收生成的示例数据，非真实资讯。

构建仍有前端单包体积提示：约 1.18 MB、gzip 371 KB，主要来自组件库。首版适用于小规模管理后台，后续可按访问情况拆分加载。真实企业微信与服务器失败回滚未做在线演练，生产环境验收见下文。

为了运行真实数据库测试，本机通过 Homebrew 安装了 MariaDB，测试实例数据放在 `/private/tmp/fetch-news-mariadb`，仅监听回环地址；未注册开机服务。测试结束后关闭该临时实例。

## 2026-09-14 生产验证

- GitHub Actions 在 Node 22、MariaDB 10.11 上完成构建、5 项单元测试、14 项集成测试、CLI/运行产物检查及浏览器完整流程，全部通过。
- 发布运行 34776974799 成功，部署 SHA 为 `eae80c2e72576c56029c51c7e44fa58199545485`，current 指向该版本并存在 `.known-good`。
- 公网 HTTPS 健康检查返回 ok，证书正常校验；子路径 SPA 深链接与资源正常，浏览器登录页正常显示，未捕获控制台 error。
- 使用服务器受保护密码文件完成公网 HTTPS 登录、会话查询、热点列表、统计和退出；无调用方 key 返回 401，退出后的旧会话返回 401。密码未输出。
- systemd 以 fetch-news 非 root 用户运行，内存约 44 MiB；Nginx 配置检测成功；3107 仅监听回环，3108 已释放。
- 可用内存 874 MiB、swap 使用 153 MiB、磁盘剩余 28 GiB；现有 Nginx、MariaDB、price-monitor、loan-console、user-dashboard 服务均 active。
- 生产仅初始化管理员，未写入验收热点或信息，未配置真实企业微信群通知；通知链路的订阅、限速、重试和恢复由模拟发送器测试覆盖。
- 按用户决定未配置数据库备份；首次发布无上一版本，生产失败回滚尚未演练。
