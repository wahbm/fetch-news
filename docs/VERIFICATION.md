# 本地验证记录

日期：2026-09-13。环境：Node.js 22.21.0、macOS、隔离的本机 MariaDB 12.3.3、Chrome。CI 已配置 MariaDB 10.11，但本次未创建远端仓库，因此没有声称远端 CI 已执行。

| 验证 | 结果 |
|---|---|
| TypeScript 类型检查、Vite 前端与 tsup 服务端构建 | 通过 |
| 5 项单元测试 | 通过：URL、日期/输入、加密、消息字节限制、WeCom 响应分类 |
| 14 项 MariaDB 集成测试 | 通过：会话/权限、最后管理员、重置 key、停用、搜索、并发去重、事务回滚、订阅匹配、限速、重试、租约恢复、子路径和登录限流 |
| 桌面浏览器完整流程 | 通过：登录、创建热点/调用方/订阅、API 提交与去重、搜索与详情、模拟通知、投递记录、退出 |
| 子路径 `/qa/fetch-news/` | 构建与相同浏览器流程通过，刷新深链接正常 |
| 390px 移动端 | 通过，侧栏折叠、页面无横向溢出，表格内部可横向滚动 |
| 编译产物 smoke test | CLI 迁移、文件传入初始密码、生产服务启动、数据库健康和 SPA 深链接通过 |
| npm audit | 0 已知漏洞（包含开发依赖）；已锁定修复版本 |
| CI/部署模板 | YAML 解析与发布脚本 bash 语法检查通过；未执行 ECS 发布 |
| 企业微信官方说明 | 直接获取官方页面，确认 4096 UTF-8 字节、20 条/分钟及全局错误码 |

所有自动通知测试注入模拟发送器，未向真实企业微信群发消息。截图使用验收生成的示例数据，非真实资讯。

构建仍有前端单包体积提示：约 1.18 MB、gzip 371 KB，主要来自组件库。首版适用于小规模管理后台，后续可按访问情况拆分加载。未对真实企业微信、生产 Nginx/systemd、阿里云网络或服务器回滚做在线验收。

为了运行真实数据库测试，本机通过 Homebrew 安装了 MariaDB，测试实例数据放在 `/private/tmp/fetch-news-mariadb`，仅监听回环地址；未注册开机服务。测试结束后关闭该临时实例。
