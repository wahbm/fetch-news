NODE_ENV=development
HOST=127.0.0.1
PORT=3000
APP_BASE_PATH=/
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=fetch_news
DB_PASSWORD=replace-locally
DB_NAME=fetch_news
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=
# Production must use HTTPS. Do not set false on public deployments.
COOKIE_SECURE=false
WORKER_ENABLED=true
